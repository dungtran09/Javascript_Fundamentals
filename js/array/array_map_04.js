const numbers = [10, 20, 30, 40, 50];

const matrixNumbers = numbers.map((number) => {
    // const arrTemp = [];
    // arrTemp.push(number);
    // return arrTemp;
    return [number];
});

console.log(matrixNumbers);
